.DS_STORE
.vscode/
**/result
.git/
.devenv/
.pre-commit-config.yaml
.playwright-mcp/
.claude/
/result-*
/docs/superpowers
/.superpowers

# Terranix scratch dir. State is remote, but a failed backend init drops a
# local copy — and it holds the UniFi API key.
/.terranix/
*.tfstate
*.tfstate.*
*.tfvars
crash.log

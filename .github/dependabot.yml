version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: daily
      time: "10:00"
    open-pull-requests-limit: 10
  - package-ecosystem: nix
    directory: "/"
    schedule:
      interval: daily
      time: "11:30"
    open-pull-requests-limit: 10
    groups:
      # Batch the nixpkgs channels (stable/unstable/nixos-unstable/nixpkgs)
      # plus home-manager and nix-darwin into a single PR — they track each
      # other and should bump in lockstep.
      nixpkgs:
        patterns:
          - "darwin"
          - "home-manager"
          - "nix-index-database"
          - "nixos-unstable"
          - "nixpkgs"
          - "stable"
          - "unstable"
      # Build/format/CI plumbing — low blast radius, frequent tiny bumps.
      dev:
        patterns:
          - "base16"
          - "disko"
          - "flake-compat"
          - "flake-parts"
          - "git-hooks"
          - "import-tree"
          - "lanzaboote"
          - "nh"
          - "nimbus"
          - "nixos-hardware"
          - "sops-nix"
          - "tinted-schemes"
          - "treefmt-nix"
